type AuthLoginResponse {
  accessToken: String!
  firstname: String!
  id: String!
  role: String!
}

type Query {
    isLogged: Boolean!
    me: Researcher!
}

input Login {
    email: String!
    password: String!
}

type Mutation {
    login(input: Login!): String!
    logout: Boolean
}