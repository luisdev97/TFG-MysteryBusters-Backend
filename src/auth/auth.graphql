type AuthLoginResponse {
  accessToken: String!
  firstname: String!
  id: String!
  role: String!
}

type Query {
    isLogged: Boolean!
}

input Login {
    email: String!
    password: String!
}

type Mutation {
    login(input: Login): AuthLoginResponse
    logout: Boolean
}